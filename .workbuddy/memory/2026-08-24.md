# 2026-08-24 工作日志

## 智学论坛 · 自学习智能体网站（共享学习平台项目）MVP 完成

**需求**：类论坛自学习智能体网站，面向小初高知识点技巧获取与输出，完成后上传 GitHub。

**交付**：项目位于 `C:\Users\张福利\WorkBuddy\共享学习平台项目`，五大功能全部实现并通过端到端 API 测试：
1. 知识点智能体自动生成（DeepSeek 接入 + 学科模板兜底，离线可跑）
2. 能力用户分享思路，被采纳赚积分（采纳奖 10 分），评分 1-5
3. 按年级 + 自定义标准（准确/速度/广度/综合）分级测评 L1-L5，按等级自适应匹配思路（示例：xy=20 求 3x+4y 有代入/换元/基本不等式/赋值 4 思路）
4. 积分经济：注册奖 20、学习耗 3、购买套餐（10/30/100 元档）、积分⇄人民币双向兑换（1 元=10 分，提现半价）
5. 自学习：反馈实时重排匹配度；/agent/autoGrow 让 AI 补充思路

**技术要点（重要，可复用）**：
- **零第三方依赖**：Node ≥22 内置 `node:http` + `node:sqlite`(DatabaseSync) + `node:crypto`（scrypt 密码哈希 + HMAC HS256 JWT），.env 手动解析。`node server/seed.js && node server/index.js` 即跑，无需 npm install。
- 本机沙箱 `cmd.exe` 被禁 → `npm.cmd` 全部静默失败/挂起；npm 安装改用 `node npm-cli.js` 且参数用 Windows 风格路径（避免 MSYS 把 `/c/...` 改成 `C:\c\...`）。
- 系统 Node 24.14.1（`C:\Program Files\nodejs\node.exe`）自带可用 npm；managed Node 22.22.2 的 npm 是坏的。
- 中文路径 + Git Bash + node 的组合有各种坑；cp 大目录到中文路径会静默失败，建议 PowerShell robocopy。

**演示账号**：teacher/teacher123（能力）、student/student123（学习）、admin/admin123。

**状态**：git 已 init 并提交首个 commit；**GitHub 推送待办**——本机无 gh、无 SSH key、无 credential helper，需用户提供 PAT（repo 权限）或手动推送。
